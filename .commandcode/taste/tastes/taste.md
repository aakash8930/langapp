# Tastes
- When wrapping `useSyncExternalStore` around localStorage-backed data parsed with `JSON.parse`, caches the raw string and parsed snapshot — comparing the raw string before re-parsing — so the getter returns a stable reference when data hasn't changed, avoiding the `Object.is` referential-inequality infinite re-render trap. Confidence: 0.6
- Wants the complete codebase analyzed and understood before anything is run or changed — prefers an analyze → plan → execute flow over jumping straight into execution. Confidence: 0.75
- Prefers work to be scoped tightly to the requested target (e.g., "web only" — run only the web app and skip unrelated surfaces such as the mobile client). Confidence: 0.6
- Invokes the command tool with `--yolo`, indicating a preference for running shell commands autonomously without confirmation prompts. Confidence: 0.4
- Dislikes redundant re-work — expects prior analysis/context to carry over across restarts and sessions rather than re-auditing the whole codebase when nothing changed. Confidence: 0.7
- Wants audit findings and current-work state recorded in a persistent note/plan file (e.g., saved under `~/.commandcode/plans/`) so work can be resumed after a restart without re-deriving context. Confidence: 0.6
- Provides feature requirements as itemized, numbered checklists and expects the assistant to audit existing implementation status against each item, then build only the remaining gaps in the same session rather than deferring to future sessions. Confidence: 0.95
- Terminates duplicate running server processes when the same project/app is running on multiple ports, killing the redundant duplicate(s) so only one instance remains. Confidence: 0.8
- References UI mockup image files (organized as UI/{locale}/web) as design specs to guide implementation before building features. Confidence: 0.9
- Treats backend security as a non-negotiable baseline (argon2id hashing, JWT rotation, rate limiting, age gates) rather than an afterthought. Confidence: 0.8
- Expects smooth animations and polished UI/UX in frontend deliverables — animations are a requirement, not optional polish. Confidence: 0.8
- Prefers end-to-end full-stack implementation that builds from design mockups through to finished, working UI. Confidence: 0.75
- Insists on production-ready code — explicitly rejects prototypes and simplifications; wants complete, shipping-quality implementations. Confidence: 0.9
- Strongly prefers reusing the existing project architecture, design system, theme variables, API layer, animation system, and auth backend over building new abstractions; resists modifying backend logic unless absolutely necessary. Confidence: 0.9
- Prefers deriving view-specific data client-side by composing existing API payloads rather than adding a new backend endpoint for each derived view — treats "no new API needed" as the default unless the existing wire data genuinely cannot express what the UI requires (e.g., recent lessons from `completedLessonIds`, "upcoming" reviews from the existing `/reviews/due` query, static hard-coded announcements when no CMS endpoint exists). When cross-reference data is bounded and stable (e.g., 104 kanji → vocab mappings), extracting it into a client-side constant file is preferred over adding API endpoints. Confidence: 0.9
- Prefers desktop-first responsive design. Confidence: 0.85
- Rejects placeholder code, TODO comments, mock APIs, fake data, and inline styles; expects fully realized implementations. Confidence: 0.9
- Prefers component-based architecture with clear separation of concerns (components/, hooks/, services/, validation/, animations/). Confidence: 0.8
- Co-locates component-specific CSS with the components that use it (e.g., `practice/practice.css` alongside `practice/HiraganaWriting.tsx`) rather than adding everything to the global stylesheet. Confidence: 0.7
- Prefers subtle, restrained animations over dramatic effects — explicitly wants "very subtle motion only, do not overdo effects"; motion serves the UI rather than drawing attention to itself. Confidence: 0.85
- Prefers real-time, as-you-type form validation (not submit-only), with error messages surfaced only after a field is touched/blurred or the user attempts to submit, rather than on initial keystroke. Confidence: 0.75
- Expects full accessibility as a baseline requirement — keyboard navigation, visible focus rings, ARIA labels, screen-reader-friendly markup, and proper autocomplete attributes — built in from the start, not added later. Confidence: 0.85
- Prefers an understated, grounded UI aesthetic; explicitly rejects glassmorphism and neon glow in favor of muted, layered palettes (e.g., Japanese Ink: charcoal, warm gray, muted gold, deep red). Confidence: 0.7
- Prefers being consulted at architectural fork points via structured decision prompts (multi-option questions with clear tradeoffs) rather than having the assistant silently choose a path — wants explicit buy-in on decisions like file-upload-vs-URL, persist-vs-ephemeral, new-endpoint-vs-client-side derivation. Confidence: 0.7
- Expects client-side security hygiene for auth forms — never store passwords locally, trim/whitespace inputs, prevent duplicate and rapid-fire submissions, and surface API and network-failure errors to the user. Confidence: 0.65
- Prefers auth/signup flows rendered as full-screen takeovers that opt out of the app shell entirely (no header, sidebar, or footer), so the entry point owns the viewport. Confidence: 0.6
- Runs dev servers in the background (`run_in_background: true`), tracks them by `taskId`, keeps them running after the invoking task completes, and provides an explicit `kill_shell({ taskId })` command so the user can tear them down on demand. Confidence: 0.8
- Prefers a two-panel split layout for auth flow pages — left panel (40%) for the form card, right panel (60%) for themed illustration/artwork — so the entry point feels spacious and brand-rich without overwhelming the form. Confidence: 0.85
- Wants a dark overlay (60–70% opacity) applied over background artwork on auth pages so the form remains the clear focal point while still showcasing the theme. Confidence: 0.8
- Prefers flat design with minimal shadows and thin borders over glassmorphism or neon effects — clean, grounded surfaces that read as premium rather than decorative. Confidence: 0.8
- Targets a premium SaaS visual aesthetic, citing Linear, Notion, and GitHub as reference applications — clean, minimalist, high-production-value UI. Confidence: 0.75
- Prefers native `<details>`/`<summary>` HTML elements for expandable/collapsible content (accordion rows, expandable cards, detail panels) over custom-built toggle/accordion components — leverages built-in keyboard accessibility and open/close state management. Confidence: 0.7
- Prefers a stabilize-then-build workflow: fix all outstanding issues → clean up temp/debug artifacts → commit → test/verify the UI → only then continue building new features; resists accumulating uncommitted, untested changes before moving forward. Confidence: 0.8
- Organizes settings/configuration pages as a two-column layout: a narrow sticky sidebar for section navigation and a wide content area that renders the active section, collapsing to a single-column horizontal-scroll nav on mobile. Confidence: 0.6
- Prefers Resend (REST API) for transactional email delivery — when building email infrastructure, Resend is the default transport over SMTP or other providers. Confidence: 0.6
- Uses fire-and-forget for non-critical side-effect notifications — notification creation in hooks (social, gamification) uses `.catch(() => {})` so failures are silently dropped rather than failing the parent operation. Confidence: 0.5
- Expects runtime verification, not just static typechecking — when asked to "run it and fix errors," spins up both API and web dev servers, tests endpoints with curl, inspects logs for boot-time crashes, and fixes runtime errors iteratively rather than stopping at compiler output. Confidence: 0.7
- Third-party service integrations (Razorpay, Resend, etc.) must gracefully degrade when API keys are not configured — the app must still boot, logging a warning rather than crashing with `getOrThrow`. Services gate on an `enabled` flag derived from key presence. Confidence: 0.75
- When the user reports a specific error (e.g., a broken import path), does not stop at fixing just the reported instance — systematically sweeps the codebase for all occurrences of the same root-cause pattern (same broken path pattern, same class of bug) and fixes them in one pass, then checks sibling directories for the same issue. Confidence: 0.8
