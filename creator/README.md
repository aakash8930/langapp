# LangApp Creator / CMS

Standalone authoring service. Set `VITE_API_URL` and sign in with a trusted administrator account. The existing content API is protected by the API's JWT and AdminGuard; creation, publishing, and audit decisions must remain server-authorized.
